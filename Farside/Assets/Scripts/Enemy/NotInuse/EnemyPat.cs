using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyPat : MonoBehaviour
{
    /*
    [hideInspector]
    public bool mustPatrol;
    public float walkSpeed;

    [SerializeField]
    private Enemy enemy;

    // Start is called before the first frame update
    void Start()
    {
        mustPatrol = true;
    }

    // Update is called once per frame
    void FixedUpdate()
    {
        if (mustPatrol) {
            Patrol();
        }
    }

    private void Patrol() {
        enemy.body = new Vector2(walkSpeed, body.velocity.y);
    }

    internal void FlipPatrol() {
        mustPatrol = false;
        enemy.Flip();
        mustPatrol = true;
    }
    */

}
